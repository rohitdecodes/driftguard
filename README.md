# DriftGuard

Minimal scaffold for the DriftGuard MVP (see PHASE_1.md).

Run the CLI scaffold:

```bash
python driftguard.py --repo . --days 30
```

This workspace contains starter files and directories. Next steps: implement `app/git_parser.py`, `app/bob_analyzer.py`, `app/scorer.py`, and the FastAPI app in `app/main.py`.
