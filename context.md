# DriftGuard — Automated Codebase Decay Monitor

> **IBM Bob Dev Day Hackathon 2026** | Solo Submission | Theme: *"Turn idea into impact faster with IBM Bob"*

---

## The Problem

Codebases decay silently. A module that was clean 3 months ago now has:
- Functions with 0 documentation, but 14 callers
- Logic that has changed 8 times with no corresponding test updates
- Variable names that made sense in v1 but are now misleading
- Complexity that has tripled since the last review

No one notices until production breaks. Static analysis tools catch syntax issues. They don't catch **drift** — the gap between what the code does now and what its documentation, tests, and naming say it does.

**DriftGuard is the first developer tool that measures codebase health as a function of change over time, not just a point-in-time snapshot.**

---

## What DriftGuard Does

```
Git History → Commit Diffs → IBM Bob Analysis → Decay Score → Dashboard
```

1. **Ingests Git history** — parses recent commits, extracts per-file diffs
2. **Sends diffs to IBM Bob** — Bob evaluates documentation drift, test drift, complexity growth, naming consistency
3. **Generates Decay Scores** — structured per-file health scores (0–100) across 4 decay dimensions
4. **Exposes a CLI** — run `python driftguard.py --repo . --days 30` for instant analysis
5. **Serves a FastAPI Dashboard** — visual health map, module-level drill-down, trend over time

---

## IBM Bob's Role (Core Engine)

Bob is not a wrapper here. Bob is the reasoning engine that does what static analysis cannot:

| Task | Bob's Contribution |
|---|---|
| Documentation drift | "Does this diff add logic without updating any docstrings or comments?" |
| Test drift | "Were tests modified when implementation changed? What's the signal?" |
| Complexity growth | "Did this change increase cyclomatic complexity, nesting, or parameter count?" |
| Naming consistency | "Are new identifiers consistent with the naming conventions in the rest of this file?" |

Every analysis prompt is logged → exported → committed to `bob_sessions/` for submission.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Backend | FastAPI (Python 3.11) |
| Git Analysis | GitPython |
| AI Engine | IBM Bob (Bob Shell subprocess, non-interactive mode) |
| Dashboard | HTML + Chart.js + Jinja2 |
| CLI | Python argparse |
| Storage | JSON files (no database needed) |
| Deployment | Local (run with `uvicorn`) |

---

## Project Structure

```
driftguard/
├── README.md
├── AGENTS.md                    ← generated by Bob /init
├── requirements.txt
├── driftguard.py                ← CLI entrypoint
├── app/
│   ├── main.py                  ← FastAPI app
│   ├── git_parser.py            ← GitPython commit/diff extraction
│   ├── bob_analyzer.py          ← Bob Shell subprocess integration
│   ├── scorer.py                ← Health score calculation
│   ├── report_generator.py      ← JSON + HTML report builder
│   └── templates/
│       ├── dashboard.html       ← Main dashboard
│       └── file_detail.html     ← Per-file drill-down
├── bob_sessions/                ← REQUIRED: Bob task exports
│   ├── session_01_planning_screenshot.png
│   ├── session_01_planning_export.md
│   └── ...
├── sample_reports/              ← Demo output for video
│   └── sample_decay_report.json
└── .bobignore
```

---

## How to Run

### Prerequisites
```bash
pip install -r requirements.txt
# IBM Bob Shell must be installed and authenticated
bob --version
```

### CLI Mode
```bash
# Analyze current repo, last 30 days
python driftguard.py --repo . --days 30

# Analyze a specific repo path
python driftguard.py --repo /path/to/your/project --days 14 --output report.json
```

### Dashboard Mode
```bash
uvicorn app.main:app --reload
# Open http://localhost:8000
```

---

## Decay Score Explained

Each file receives a **Health Score from 0–100** across 4 dimensions:

| Dimension | Weight | What It Measures |
|---|---|---|
| Documentation Drift | 30% | Documentation staleness vs code changes |
| Test Coverage Drift | 30% | Test updates vs implementation changes |
| Complexity Growth | 25% | Function length, nesting, param growth over time |
| Naming Consistency | 15% | New identifiers vs established conventions |

**Final Health Score = weighted average of all dimensions**

- 🟢 80–100: Healthy
- 🟡 60–79: Watch
- 🟠 40–59: At Risk
- 🔴 0–39: Critical Decay

---

## Sample Output

```json
{
  "repo": "my-project",
  "analysis_window_days": 30,
  "analyzed_at": "2026-05-02T14:32:00Z",
  "files": [
    {
      "path": "app/auth/login.py",
      "health_score": 34,
      "status": "CRITICAL",
      "commit_count": 12,
      "dimensions": {
        "documentation_drift": 18,
        "test_drift": 22,
        "complexity_growth": 45,
        "naming_consistency": 71
      },
      "bob_analysis": "This file has changed 12 times in 30 days. No docstring updates detected. The function `validate_token` has grown from 8 to 31 lines with 3 new parameters, none of which appear in existing tests. The naming `_tmp_check` introduced in commit a3f91c violates the established `validate_` prefix convention.",
      "recommendation": "Prioritize test update and documentation review for validate_token before next release."
    }
  ],
  "summary": {
    "total_files_analyzed": 18,
    "critical": 3,
    "at_risk": 5,
    "healthy": 10
  }
}
```

---

## Why This Wins

**Creativity**: No other tool measures decay *over time*. Every existing tool (SonarQube, pylint, radon) does point-in-time analysis. DriftGuard's temporal framing is genuinely novel.

**Effectiveness**: The output is immediately actionable. A dev sees their 3 red files and knows exactly where to focus before the next release.

**Bob Integration**: Bob is doing qualitative reasoning that static tools cannot — "did the documentation intention match what changed?" is a semantic judgment, not a syntax check.

**Completeness**: End-to-end working demo — CLI input to dashboard output — using a real repository.

---

## Submission Checklist

- [ ] Working CLI + Dashboard
- [ ] `bob_sessions/` with all task exports (screenshots + `.md`)
- [ ] `AGENTS.md` generated by Bob `/init`
- [ ] Public GitHub repo
- [ ] Video demo ≤ 3 minutes
- [ ] Written problem + solution statement (≤500 words)
- [ ] IBM Bob usage statement
- [ ] Submitted before May 3, 10:00 AM ET

---

*Built solo in 48 hours using IBM Bob as the core AI development partner.*
